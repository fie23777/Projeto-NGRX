import { EntradaSaidaPipesPipe } from './entrada-saida-pipes.pipe';

describe('EntradaSaidaPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new EntradaSaidaPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
